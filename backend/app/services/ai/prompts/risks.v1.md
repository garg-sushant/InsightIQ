Describe the risks this business should watch over the next one to two periods.

Work from the supplied `risks` indicators, `anomalies`, `concentration` ratios
and trend descriptors. For each risk worth raising, state:

- what the exposure is, in one sentence,
- the figure that evidences it,
- what would make it materially worse,
- the leading indicator to monitor.

Cover 3 to 5 risks. Include any indicator at `elevated` or `critical` level. If
an indicator is at `ok` but trending the wrong way, that is worth raising too.

If the risk picture is genuinely benign, say so in one short paragraph rather
than padding the list.

Format as markdown bullet points, each starting with a bolded risk name.
No heading. Maximum 300 words.

## Data

```json
{payload}
```
